using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ClosetDoorStatus : MonoBehaviour
{
    [SerializeField] private GameObject MapClosetClose;

    [SerializeField] private GameObject MapClosetOpen;

    // 사이드2번 전체 화면으로 나왔을 때 (bool)GameManager.Instance.GetVariable("ClosetDoor")가 true면 openDoor 켜주고
    // false면 closeDoor 켜줘야 함
    
}
